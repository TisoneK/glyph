# Agent + Model Registry (update in place)

Which agents and models have worked on this repo.

| Agent | Model | First seen | Last seen | Sessions |
|---|---|---|---|---|
| Claude Code | claude-opus-4-8 | 2026-07-29 | 2026-07-31 | 3 |
| Super Z | glm-5.2 | 2026-07-30 | 2026-07-30 | 2 |
| Buffy | deepseek-v4-flash | 2026-08-01 | 2026-08-01 | 8 |

## Observations

- **Claude Code / claude-opus-4-8:** bootstrapped the repo + `.context/` (core 0.3.0) and authored RESEARCH.md (scope, technique catalog, architecture, phasing). (2026-07-29)
- **Claude Code / claude-opus-4-8 (Session 4):** built the general-purpose `glyph-re` package end-to-end — 10 pipeline stages as subpackages over a shared SQLite catalog, `glyph` CLI, 32 passing tests. Recorded ADR-2 (architecture) + ADR-3 (standalone). Worked cleanly with per-stage commits (10 logical commits, each pushed). Ran the suite in a `.venv` on system Python 3.9.6. (2026-07-30)
- **Claude Code / claude-opus-4-8 (Session 5):** added the HITL review workflow (`glyph.review` + `glyph review` CLI + catalog `review_state` migration). 45 tests. Handled a mid-session concurrent push from the user (docs edit to `origin/main`) with a clean `git rebase origin/main` — confirmed zero file overlap first, per the Git Workflow rule. (2026-07-30)
- **Super Z / glm-5.2:** Z.ai cloud sandbox agent (Linux, `/home/z/my-project/glyph` workspace). System prompt identifies the agent as "Super Z, built on the GLM model developed by Z.ai" but does not state the exact version — Session 2 recorded `unknown` per Pitfall #25 (never guess). User confirmed `glm-5.2` in Session 3; corrected here. Ran Session 2 (`.context/` E2E read-through, root AGENTS.md enhancement, one `[core-defect]` override for `context.schema.json` `coreVersion` drift) and Session 3 (Glyph deep-dive research: 5 parallel online-research clusters → `RESEARCH-DEEP-DIVE.md` at repo root, 76 KB / 612 lines / 11 sections; Rosetta prior-art report; resolved 4 of 5 RESEARCH.md §11 open questions; scoped the Phase-0 proof concretely). Observed: parallel sub-task pattern works well for online research — keep each agent's scope narrow (Tasks 6+7 timed out as single large agents on glm-5.2; re-launched as 3 tighter-scope sub-tasks on the haiku model, all completed). (2026-07-30)
- **Claude Code / claude-opus-4-8 (Session 10):** built `glyph.sensitive` (sensitive-data/endpoint/risk flagging) as a modular stage. Key correction captured: for an RE tool, flag-and-locate + keep values — do not redact captured data by default (redaction is opt-in export only). 88 tests. (2026-07-31)
- **Buffy / deepseek-v4-flash (Session 20):** fixed the TUI's E2E wiring (CLI → live capture → Textual dashboard): stage opt-out flags (`--no-sensitive`/`--no-snihunt`/`--snihunt-no-net`) now honored by the live dashboard's own analysis; failed captures show `✗ failed · <error>` in the header; auto-mode `capture_url` + headless `run live`/`capture live` emit progress lines; url-required check moved before TUI takeover. 156 pass / 8 skip. Used the code-reviewer-deepseek-flash sub-agent twice in parallel with test runs — both passes caught real issues (timing-flaky tests → rewrote to poll with a deadline; dead `_status()` after the `_capture_state()` batching; `glyph capture live` auto path missing `progress=`). Lesson reinforced: parallel review + test while editing is a cheap correctness multiplier. (2026-08-01)
- **Buffy / deepseek-v4-flash (Session 21):** fixed `pip install -e '.[dev]'` on the Mac — mitmproxy 10+ needs Python >=3.10, the Mac runs 3.9.6, so the `live` extra floor was lowered to `mitmproxy>=9` (pip still resolves the latest on newer Pythons). Verified the user's exact failing command now installs cleanly (mitmproxy 9.0.1 + Rust wheel); 159 pass / 5 skip. Also handled a user-identity correction (the user is Tisone Kironget; "bao" is only the macOS account name) by clarifying environments.md. Reviewer's parallel pass caught a real `.context` bug: my str_replace duplicated the `- **OS:**` line in environments.md — fixed. Lesson: str_replace on update-in-place memory files can duplicate lines when the newString re-adds lines the oldString already covered; re-read the block after editing. (2026-08-01)
- **Buffy / deepseek-v4-flash (Session 22):** installed the Playwright Chromium browser binary in the Mac venv (`.venv/bin/playwright install chromium` — closed the Session 21 open item) and smoke-tested `glyph capture live https://example.com` end-to-end (46 flows + 46 DOM labels, exit 0). The smoke test surfaced a real display bug: the 'by type' summary line listed every resource type TWICE (the driver tags response flows `playwright:<type>` and request flows `playwright:request:<type>`; `report_live` printed each source key verbatim and `_types_line` used a last-wins dict comprehension that dropped one side). Fixed with a shared `by_type()` aggregator (sums both sides per type) used by both display sites + 2 regression tests. 161 pass / 5 skip. Reviewer reviewed in parallel with tests (3 passes, clean; nits: module-level import placement + `dict[str, int]` annotations). Lesson: a live smoke test is the cheapest way to surface display bugs mock tests can't — the doubled by-type line looked fine in unit fixtures. (2026-08-01)
- **Buffy / deepseek-v4-flash (Session 23):** verified the live Textual dashboard ON-DEVICE on the Mac (the open item carried since Session 15): drove `glyph run live https://example.com` in a real pty via a Python harness (tmux not installed; expect/script available but the pty harness gave full winsize + timed keystroke control). Confirmed: `● LIVE mm:ss` header at t=1.6s, FLOWS streaming 0→2→22→23→46 over ~13s, `✓ captured` at t=14.8s, `q` quit cleanly at t=22.8s, catalog persisted 46 flows + 1 page for target example.com. No failures, no tracebacks — the capture-worker + WAL + 1s/4s tick design works on-device. The Windows-box verification is the only remaining live-path gap. Lesson: a pty harness is a solid tmux substitute for verifying full-screen TUIs when tmux isn't installed. (2026-08-01)
- **Buffy / deepseek-v4-flash (Session 24):** built the parallel analysis pipeline (ADR-15) after the user asked why the post-capture stages (schema, rosetta, sensitive, snihunt) wait for each other. The real dependency graph has ONE edge (schema→rosetta: dom_attribute reads schema's enum candidates), so the stages now run as three concurrent lanes (new glyph/pipeline.py::run_analysis) — snihunt, the slowest (network recon), no longer waits. The parallelization also FIXED a latent bug Session 23's verification had exposed: the TUI's analysis workers opened a fresh Catalog with no active target, so fields/dictionary/findings silently landed in the (unassigned) bucket (target show: findings 0/fields 0/dictionary 0 while the dashboard displayed them). Per-lane set_target fixes it for CLI + TUI. 165 pass / 5 skip. Reviewer ran 4 parallel passes (clean). Lesson: a user's 'why does this wait?' question is often a real data-dependency question — verify the actual graph before defending sequential order. (2026-08-01)
- **Buffy / deepseek-v4-flash (Session 25):** profiled ADR-15's wall-clock win with throwaway benchmarks (/tmp/prof_har.py, /tmp/prof_io.py): before = old _gather order reconstructed, after = run_analysis(). Offline/CPU-bound: 1.00x (schema 54% + sensitive 36% are pure-Python CPU the GIL serializes; zero regression). Controlled I/O (fixed sleeps on CT/DNS/reverse-IP, 2.5x HAR): 1.44x deterministic — snihunt's ~12s network I/O dominates and the ~6s of CPU stages hide under it; parity identical both ways (1068/54/574/14 findings). Real network: overlap mechanism proven in the timeline but external CT-log jitter (2-9s/call) swamps the number. Conclusion: ADR-15's win is I/O-bound-only. Lesson: on CPython, thread-pool parallelism only pays for I/O-bound lanes; and a deterministic-fake network module is the way to measure I/O overlap without jitter.
- **Buffy / deepseek-v4-flash (Session 26):** fixed the display bug where tables (flows/dict/sensitive/dashboard) fetched ALL targets' rows. Root cause: the active target was in-memory ONLY, so every fresh Catalog a display command opened had no active target and reads fell back to all targets. Fix: persist active_target_id in meta; display/stage commands + TUI reads opt into Catalog(restore_active=True); run/capture write paths stay pristine (ADR-12 unchanged). glyph target list marks the current target; target show persists the switch. Also fixed latent unassigned-bucket writes for standalone sensitive/snihunt/schema/rosetta and the reviewer-caught set_active_target(0) nuking a real persisted target. 171 pass / 5 skip (+5 tests). Lesson: when a data layer has per-entity filters but consumers open fresh instances, the bug is in the wiring, not the store — trace who opens the Catalog.
- **Claude Code / claude-opus-4-8 (Session 11):** set up the local Windows development environment — created `.venv` (Python 3.14.2), installed `glyph-re[dev]`, downloaded Playwright Chromium, and verified the baseline (`93 passed, 1 skipped`). Discovered and worked around a Windows CRLF/LF line-ending mismatch in `.context/core/` that caused `context-sync verify` to fail on the vendored core (the manifest hashes are computed against LF-only blobs). Added `.gitattributes` (`eol=lf`) and a Windows environment block to `environments.md`. (2026-07-31)
- **Buffy / deepseek-v4-flash (Session 27):** TUI overhaul. Root-caused the pre-existing "squeezed top-left" home page to a Textual 8.2.8 quirk: only pushed/switched screens get their CSS loaded (`_load_screen_css`); the DEFAULT screen's CSS silently never applies. Fix: all screen CSS moved to GlyphApp.CSS with type-scoped selectors + a CSS regression guard test. Added stage-selection checkboxes (all ON except vpndec) + a vpndec file input on the home page; run_analysis gained no_schema/no_rosetta; the dashboard got a compact brand row and clipped host cells. 177 pass / 5 skip. Lesson: default-screen CSS in Textual is dead styling — assert computed region/CSS in tests or layout regressions go unnoticed. (2026-08-01)
